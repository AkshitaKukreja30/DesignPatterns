Builder

example: subway. You choose what you want: bread, suaces, veggies and then they are customised using IBuilder and assembled together by IDirector

client class --> IDirector and we specify the types example: LuxuryHouse

Director --> IBuilder invoking methods BuildProductA(), BuildProductB(), BuildProductC() etc